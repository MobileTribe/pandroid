<?xml version="1.0"?>
<template
        format="5"
        revision="6"
        name="Basic PandroidFragment"
        minApi="14"
        minBuildApi="14"
        description="Creates a new basic PandroidFragment with a presenter.">

    <category value="Pandroid"/>
    <formfactor value="Mobile"/>

    <parameter
            id="fragmentClass"
            name="Fragment Name"
            type="string"
            constraints="class|unique|nonempty"
            default="MainFragment"
            help="The name of the fragment class to create"/>

    <parameter
            id="layoutName"
            name="Layout Name"
            type="string"
            constraints="layout|unique|nonempty"
            suggest="fragment_${classToResource(fragmentClass)}"
            default="fragment_main"
            help="The name of the layout to create for the fragment"/>


    <parameter
            id="packageName"
            name="Package name"
            type="string"
            constraints="package"
            default="com.leroymerlin.pandroid"/>

    <parameter
        id="parentActivityClass"
        name="Hierarchical Parent"
        type="string"
        constraints="class|exists|empty"
        default=""
        help="The hierarchical parent activity or fragment used to register your fragment" />


    <globals file="globals.xml.ftl"/>
    <execute file="recipe.xml.ftl"/>

</template>
